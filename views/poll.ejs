<!DOCTYPE html>
<html lang='en'>
  <head>
    <title>Schoodle</title>
    <% include partials/_header %>
  </head>

  <body>
    <main class='event-jumbo'>
      <div>
        <h1><%= eventname %></h1>
        <h2><%= eventdate %></h2>

        <table class='poll-results'>
          <tr>
            <th>Name</th>
            <% times.forEach(time => { %>
              <th><%= time %></th>
            <% }); %>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
          
          <% for (let person in votes) { %>
            <tr>
              <td><%= person %></td>
              <% votes[person].forEach((vote) => { %>
                <% if (vote) { %>
                  <td class='vote true'>Y</td>
                <% } else { %>
                  <td class='vote false'>N</td>
                <% } %>
              <% }) %>
              <td><button class='edit-vote' person='<%= person %>'><i class="fa fa-pencil"/></button></form></td>
              <td><button class='delete-vote' person='<%= person %>'><i class="fa fa-trash-o"/></button></form></td>
            </tr>
          <% } %>
        </table>
      </div>
    </main>
  </body>
</html>
